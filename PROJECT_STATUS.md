# Project Status

## 已完成

- 已增加运行版本一致性：`/api/health` 返回 `running_build_id`、`disk_build_id`、`stale`；启动器只复用版本一致的本项目实例，前端发现旧后台会清空旧数据并自动请求本地重启。
- 已补充 B 站登录基础链路：设置页可申请二维码、显示本地二维码、轮询扫码状态和退出登录；登录凭据使用 Windows DPAPI 加密保存到用户 AppData，不读取浏览器 Cookie，接口请求会优先使用该登录态。
- 已按当前登录 B 站网页核对并调整投稿、合集目录和合集详情接口参数；合集目录和合集详情仍复用真实分页数据。
- 单视频解析、封面本地代理、下载任务接口和启动器现有功能未做无关改动。

## 当前验证结果

- 本地页面打开后显示“本地服务已连接”，健康接口当前 `running_build_id == disk_build_id` 且 `stale=false`。
- 连续运行两次启动器保持同一 `instance_id`，未启动重复实例；修改源码后，已有本地页面的健康轮询能触发后台更新，更新后重新达到版本一致。
- 已登录 B 站网页实际请求：投稿页使用 `/x/space/wbi/arc/search`，当前目标主页响应为 `ps=40、pn=1、count=18`；空间导航显示的“投稿 276”是页面其他内容统计，不能直接当作视频投稿接口总数。合集目录使用 `/x/polymer/web-space/seasons_series_list`，返回 14 个合集；首个合集使用 `/x/polymer/web-space/seasons_archives_list`，返回 66 个视频、3 页。
- 本地 `/api/up/collections` 返回 200、总数 14；`/api/up/collection` 返回 200、总数 66、3 页、首屏 30 条。
- 匿名 `/api/up` 仍会间歇性触发 B 站 `-352/-412` 风控；现在返回中文“请在设置中扫码登录后重试”，不再用错误总数或空列表伪装成功。
- 扫码登录已自动申请官方二维码；本地 `/api/login/qr.svg` 返回 200，轮询状态已验证为“等待扫码”。尚未代替用户完成扫码确认。
- 单视频回归：真实视频 `BV1eQNL6JEV2` 解析成功，标题、UP 主和 1 个分 P 正常；封面代理返回 200/JPEG；任务接口返回 200。

## 未完成 / 需人工确认

- 由于当前匿名投稿接口风控，且本轮没有用户实际扫码，尚未声称目标 UP 主登录后的第 1 页、中间页、最后一页、跨页选择和批量下载完整通过。
- 需要用户在设置页扫码一次后，继续验证登录后的投稿总数、完整分页、合集/系列、批量 MP4/M4A；扫码成功分支和登录凭据在本轮自动测试中未完成。
- 未开发专栏、字幕、弹幕、4K 等扩展功能。

## 测试

- `python -m py_compile app.py`、`node --check static/app.js`、`git diff --check` 通过。
- 已做本地健康、版本自动更新、重复启动复用、二维码 SVG、真实解析、封面代理和任务接口检查。
- 浏览器 Network 已核对目标主页投稿、合集目录和合集详情的真实请求及分页字段；未读取或保存浏览器 Cookie。
