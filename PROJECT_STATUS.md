# Project Status

## 已完成

- CourseFlow 腾讯会议单回放已接入独立浏览器会话：使用 AppData 下专用 Edge/Chrome Profile，不读取日常浏览器资料；官方扫码成功后自动关闭窗口。
- 新增腾讯会议登录状态、退出登录和异步单回放解析；只接受 `meeting.tencent.com` 的 HTTPS `/cw/`、`/crm/` 链接。
- 解析从当前回放页捕获 MP4 请求及允许转发的请求头；链接和凭据仅存当前进程内存，限时 30 分钟，不写配置/日志、不返回页面。页面只显示标题、时长和是否捕获唯一流。
- B 站和转写执行流程未改；浏览器依赖按需放入当前 AppData 的 Python 包目录，不下载或安装浏览器。

## 本轮验证

- `python -m py_compile app.py meeting_browser.py build_info.py`、`node --check static/app.js`、`node --check static/asr.js`、`git diff --check` 通过。
- 使用隔离 AppData 启动本地服务，`/api/health` 和 `/api/meeting/status` 正常；接口拒绝非腾讯会议域名；页面由真实无头 Chrome 打开，登录/解析控件可用且无 JavaScript 页面异常。
- 启动日志曾因开发中临时缩进语法错误失败；已修正，并用隔离数据目录重新启动验证通过。
- 尚未完成真实微信扫码、真实登录态识别及指定回放 MP4 捕获；需要用户在官方登录窗口扫码，并用有权访问的回放进行实测。

## 尚未实现 / 问题

- 腾讯会议媒体下载、转写、批量导入不属于本阶段，仍未接入。
- 扫码窗口真实运行以及真实回放页面/MP4 流尚待用户账号环境验收；验证码或额外人工确认不能自动处理。

## 下一步

- 用户在腾讯会议设置中启动专用窗口并微信扫码；用一条有访问权的 `/cw/` 或 `/crm/` 回放验证登录信号、标题/时长及 MP4 请求上下文。
- 后续阶段再接入媒体下载、转写和批量导入。
