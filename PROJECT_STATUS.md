# Project Status

## 当前成果

- B 站自动转写与本地音频转写共用持久化任务系统；任务创建时固定 provider/model，FunASR、MiMo v2.5、MiMo v2.6 可用。
- 本轮新增转写任务取消 API：队列任务可立即取消；B 站临时下载请求停止；本地 ASR 复用删除接口；MiMo 在当前 HTTP 请求返回后停止，不强杀线程，并在每个片段/重试前检查取消。
- MiMo v2.5/v2.6 共用进程内串行队列；FunASR 不受影响，等待任务显示队列位置，取消排队任务不请求云端。
- MiMo 请求日志包含任务/provider/model、片段、字节数、开始时间、耗时、尝试次数、状态、重试类别及安全响应元数据；不记录密钥、音频、请求体或正文。运行任务阶段展示当前片段、尝试次数和真实等待秒数。

## 本轮诊断与验证

- 检查提交 `08d47e4`：只增加片段进度回调及相关状态更新；没有改变 Flash 请求参数、切片时长、网络重试或并发逻辑。回调会增加少量本地历史写入，但不能据此认定 API 变慢。
- 当前服务可见的 MiMo 任务均已完成，无法还原某次慢请求的精确原因；仍需用请求日志区分实际 provider/model、此前任务并发、429/5xx/重试及网络/API 延迟。本轮没有调用计费 MiMo API。
- 已通过 Python/JavaScript 语法检查、`git diff --check`；隔离模拟验证 MiMo 队列串行与排队取消、当前片段结束后停止后续请求、FunASR DELETE 取消、B 站下载停止调用、429→成功重试日志脱敏及取消 API 状态码。
- Chromium 浏览器实测取消确认、立即显示“正在取消”并刷新后端状态；等待时间本地递增且不额外请求接口。浏览器交互使用隔离模拟数据。

## 尚未验证

- 未对真实正在运行的 BBDown 下载或 MiMo HTTP 请求执行取消；urllib 的在途请求无法安全强杀，取消会等待它返回后停止后续请求。
- 本轮没有真实 MiMo API 调用；真实 API 延迟原因须由新增安全日志观测。

## 下一步

- 下次运行短时受控 MiMo 任务时检查每片耗时、重试和 provider/model；避免为诊断重复运行长音频。
